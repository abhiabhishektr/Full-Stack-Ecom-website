<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Animated Pie Chart</title>
    <style>
        .pie-chart {
            position: relative;
            width: 200px;
            height: 200px;
            border-radius: 50%;
            clip-path: inset(0);
        }

        .segment {
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
        }

        .blue {
            background-color: blue;
            clip-path: polygon(50% 50%, 100% 0%, 100% 50%);
        }

        .red {
          background-color:red;
          clip-path:polygon(50% 50%,100% -10%,100% -10%);
          animation:red-fill ease-in-out forwards infinite alternate-reverse; 
          animation-duration :2s; 
          
      }
      
      @keyframes red-fill{
          from{
              clip-path:polygon(50% -10%,100% -10%,100% -10%);
          }
          
          to{
              clip-path:polygon(50%-10%,100%-10%,90%-20%);
              
              
              
              
              
              
              
              }
      }
      
        
    </style>
</head>
<body>

<div class="pie-chart">
    <div class="segment blue">fghddh</div>
    <div class="segment red">hfdghfg</div>
</div>

</body>
</html>
